# JavaSwingJobPlatfrom
JobPlatformGUI is a Java Swing-based recruitment application. Using a single-frame design, it features role-based access for Admins, Companies, and Job Seekers. The system handles input validation, catches errors cleanly, and syncs runtime data arrays directly to local text files.
